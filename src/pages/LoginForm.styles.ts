import { TextField } from '@material-ui/core';
import styled from 'styled-components';

export const SyteldForm = styled.form`
    width: 100%
    display: flex;
    justify-content: center;
    align-items: center;
    
`;
